<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.city.mapper.UserRechargeRecordMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.city.bean.UserRechargeRecord">
        <id column="id" property="id" />
        <result column="recharge_amount" property="rechargeAmount" />
        <result column="time" property="time" />
        <result column="recharge_method" property="rechargeMethod" />
        <result column="userid" property="userid" />
        <result column="way" property="way" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, recharge_amount, time, recharge_method, userid, way
    </sql>

</mapper>
